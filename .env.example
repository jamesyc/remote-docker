# Generate a preauthorized auth key in your Tailscale admin console.
# Prefer an ephemeral=false, reusable=false key with a restrictive tag.
TS_AUTHKEY=tskey-auth-REPLACE_ME
TS_HOSTNAME=gpu-devbox

# Your public SSH key, not your private key.
PUBLIC_SSH_KEY=ssh-ed25519 REPLACE_ME james@example

